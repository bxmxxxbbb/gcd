CMAKE_MINMUM_REQUIRED(VERSION 3.16)
PROJECT(XMCurl)

SET(CMAKE_BUILD_TYPE "Release")

INCLUDE_DIRECTORIES(include)

SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -O3 -Wall")

AUX_SOURCE_DIRECTORY(src SRC_LIST)

ADD_LIBRARY(XMCurl ${SRC_LIST})