CMAKE_MINIMUM_REQUIRED(VERSION 3.16)
PROJECT(XMPthreadPool)

SET(CMAKE_BUILD_TYPE "Release")

INCLUDE_DIRECTORIES(include ${XMList}/include)

SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -O3 -Wall")

AUX_SOURCE_DIRECTORY(src SRC_LIST)
AUX_SOURCE_DIRECTORY(${XMList}/src SRC1_LIST)

ADD_LIBRARY(XMPthreadPool SHARED ${SRC_LIST} ${SRC1_LIST})

target_link_libraries(XMPthreadPool pthread)